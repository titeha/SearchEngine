{
  "SearchEngineService": {
    "Sources": {
      "postgres-demo": {
        "IsEnabled": true,
        "Provider": "postgres",
        "ConnectionStringName": "POSTGRES_DEMO",
        "Query": "select id, text from search_documents order by id"
      }
    }
  }
}